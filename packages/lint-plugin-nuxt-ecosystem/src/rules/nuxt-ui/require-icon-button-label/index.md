# `@nustack/nuxt-ui/require-icon-button-label`

Icon-only `UButton` instances need an accessible label. `label`, `aria-label`, `title`, or
visible slot content satisfies the rule.

```vue
<UButton icon="i-lucide-search" aria-label="Search" />
```
